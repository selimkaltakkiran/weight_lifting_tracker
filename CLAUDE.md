# Stack — Progression Tracker

## Working preferences

- Do not automatically start the dev server / browser preview to verify UI changes after edits. Skip the verification workflow by default. Only launch a preview or verify in-browser if explicitly asked to.
